<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.metoo.nspm.core.mapper.nspm.VendorMapper">

    <resultMap id="Base_Result_Map" type="com.metoo.nspm.entity.nspm.Vendor">
        <id column="id" property="id"></id>
        <result column="addTime" property="addTime"></result>
        <result column="name" property="name"></result>
        <result column="index" property="index"></result>
    </resultMap>

    <sql id="Base_Column_list">
        id, addTime, deleteStatus, `name`, index
    </sql>

    <select id="selectObjById"  parameterType="java.lang.Long" resultMap="Base_Result_Map">
        SELECT
          *
        FROM
          metoo_vendor
        WHERE
          id = #{id}
    </select>

    <select id="selectObjByName"  parameterType="java.lang.String" resultMap="Base_Result_Map">
        SELECT
          *
        FROM
          metoo_vendor
        WHERE
          name = #{name}
    </select>


    <select id="selectConditionQuery" parameterType="java.util.Map" resultMap="Base_Result_Map">
        SELECT *
        FROM metoo_vendor
        ORDER BY `index` ASC
    </select>

</mapper>